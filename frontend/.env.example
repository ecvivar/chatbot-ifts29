# URL de la API del backend desplegado, por ejemplo:
# VITE_API_URL=https://asistente-virtual-api.vercel.app
#
# Si se deja vacía:
#   - En desarrollo local se usa por defecto http://localhost:3000
#   - En producción se usa la misma URL relativa (mismo dominio)
VITE_API_URL=